# OpenWeather API key — required by the BFF route /api/weather.
# Generate one for free at https://openweathermap.org/api (it can take up to
# 2 hours to activate after sign-up).
OPENWEATHER_API_KEY=
